# Цель этого файла — аудит зависимостей, а не установка.
#
# Ставится пакет через `pip install partest-gen`, состав объявлен в `setup.py`.
# Но `pip-audit` читает зависимости только из `pyproject.toml` или из
# `requirements.txt`: объявленные императивно в `setup.py` он статически не видит
# и честно отвечает «не проверено».
#
# Пока файла не было, ворота писали «pip-audit НЕ ПРОВЕРЕНО» — и это неотличимо
# от «уязвимостей нет» ровно настолько, насколько читатель невнимателен. Тот же
# приём у partest и у partest-load.
#
# Перечислено обязательное из `install_requires`. Средства разработки из extra
# `dev` сюда не идут: они не уезжают к потребителю, а аудит нужен про то, что
# у него установится.

partest>=2.0.0
pyyaml>=6.0.2
requests>=2.31.0
